version: 0.2

phases:
  install:
    runtime-versions:
      python: 3.8
    commands:
      - cd ./tests
      - pip3 install -r requirements.txt
  post_build:
    on-failure: ABORT
    commands:
      - python3 -m pytest